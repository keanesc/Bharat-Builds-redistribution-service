{
  "name": "rescue-radius",
  "private": true,
  "version": "0.1.0",
  "workspaces": ["frontend", "backend", "infra", "shared"],
  "scripts": {
    "build": "npm run build --workspaces",
    "typecheck": "npm run typecheck --workspaces",
    "test": "npm run test --workspace backend",
    "seed": "npm run seed --workspace backend",
    "cdk:synth": "npm run synth --workspace infra",
    "cdk:diff": "npm run diff --workspace infra",
    "cdk:deploy": "npm run deploy --workspace infra"
  },
  "engines": {
    "node": ">=20"
  }
}
